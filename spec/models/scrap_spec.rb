require 'rails_helper'

describe Scrap do
  describe ".initialize" do
    context "defaults" do
      # subject(:fare) { Scrap.new("HNL", "PDX") }
      # specify        { expect(fare).to eq(2) }
    end
  end
end
